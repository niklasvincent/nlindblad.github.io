{% block system %}
user www-data;
error_log  /tmp/error.log;
pid        /tmp/nginx.pid;
worker_processes              auto;
worker_priority               -5;
worker_rlimit_nofile          100000;

events {
    worker_connections        2048;
    multi_accept              on;
    use                       epoll;
}
{% endblock %}
